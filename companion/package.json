{
  "name": "vidcrawl-companion",
  "version": "0.1.0",
  "description": "Desktop companion app that manages the VidCrawl local API server",
  "scripts": {
    "dev": "cargo tauri dev",
    "build": "cargo tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  }
}
